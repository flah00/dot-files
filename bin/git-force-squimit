#!/bin/bash
current=`git rev-parse HEAD`
master=`git rev-parse master`
[ $current = $master ] && (echo "Will not run on master"; exit 1)

revs=`git cherry -v origin/master | wc -l`
git add .
git commit -am ok
git rebase -i HEAD~$revs
git push -f
